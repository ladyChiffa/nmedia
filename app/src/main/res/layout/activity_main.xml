<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/padding_global"
    tools:context=".activity.MainActivity"
    >

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="0dp"
        android:layout_height="0dp"

        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/card_post"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/editing_header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
    />

    <androidx.constraintlayout.widget.Group
        android:id="@+id/group"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:constraint_referenced_ids="editing,cancel_edit,editing_mark,editing_header"
        />
    <ImageView
        android:id="@+id/editing_mark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/outline_edit_square_24"
        app:tint="@android:color/holo_purple"

        android:background="?selectableItemBackground"
        app:layout_constraintTop_toTopOf="@id/editing_header"
        app:layout_constraintBottom_toBottomOf="@id/editing"
        app:layout_constraintStart_toStartOf="parent"
        android:paddingEnd="@dimen/padding_self"
        />
    <TextView
        android:id="@+id/editing_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?selectableItemBackground"
        android:singleLine="true"
        android:textColor="@android:color/holo_purple"

        app:layout_constraintEnd_toStartOf="@id/cancel_edit"
        app:layout_constraintStart_toEndOf="@id/editing_mark"
        app:layout_constraintBottom_toTopOf="@id/editing"

        android:text="@string/edit_header"
        />
    <TextView
        android:id="@+id/editing"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?selectableItemBackground"
        android:singleLine="true"

        app:layout_constraintEnd_toStartOf="@id/cancel_edit"
        app:layout_constraintStart_toEndOf="@id/editing_mark"
        app:layout_constraintBottom_toTopOf="@id/content"

        tools:text="@sample/posts.json/data/content"
        />
    <ImageView
        android:id="@+id/cancel_edit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/outline_cancel_24"
        app:tint="?colorAccent"
        android:background="?selectableItemBackground"
        app:layout_constraintTop_toTopOf="@id/editing_header"
        app:layout_constraintBottom_toBottomOf="@id/editing"
        app:layout_constraintEnd_toEndOf="parent"
        android:paddingStart="@dimen/padding_self"
        />


    <EditText
        android:id="@+id/content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/save"
        app:layout_constraintBottom_toBottomOf="parent"
        android:padding="@dimen/padding_global"

        android:importantForAutofill="no"
        android:inputType="textMultiLine"
        android:hint="@string/content"
        android:background="@android:color/transparent"
        />

    <ImageView
        android:id="@+id/save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_save_48"
        app:tint="?colorAccent"
        android:background="?selectableItemBackground"
        app:layout_constraintBottom_toBottomOf="@id/content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/content"
         />

</androidx.constraintlayout.widget.ConstraintLayout>